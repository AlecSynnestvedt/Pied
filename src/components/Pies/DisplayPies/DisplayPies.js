const DisplayPies = (props) => {
  console.log(props);
    return(
    <>
    {
      props.pies.map((pie, key) => {
        return(
          <tr key={key} >

          </tr>
        )
      })
    }
    test 
    <>
  );
};

export defaults 